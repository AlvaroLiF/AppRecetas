import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.spanishfoodapp',
  appName: 'AppRecetas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
